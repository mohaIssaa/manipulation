In config, parameters to be tuned are saved there.

In training.py it functions the training process based on chosen model.

In unet1d, models that were validated via an entire performance loop are placed. Evaluation currently uses simple action/success or not logic.

todo: more suitable models for the data format in simplified version are expected to be the next work goal.
